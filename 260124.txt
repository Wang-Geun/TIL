오늘의 작업
1. github와 로컬 컴퓨터를 연결하여 원격 연결 성공
    -순서
    1) git init
    2) git remote add origin https://github.com/Wang-Geun/TIL.git
    3) git remote -v (선택사항)
    4) git pull origin master
    5) git push --set-upstream origin master

2. ssafy git python 4차 LV.4 문제 해결
    1) 전 단계에서 API 호출을 통해서 데이터를 받아와 .json()을 통해 딕셔너리로 받아온 데이터를 빈 리스트에 키 값을 임의로 정하고 넣음
    2) 이후에 빈 리스트에 들어간 딕셔너리 데이터 A와 B를 새롭게 키와 밸류로 지정하는 단계에서 함수 C를 하나 더 정의함.
느낀 점: 함수 안에 들어가는 함수 C를 만드는 과정을 처음 겪어 문제를 푸는데 굉장히 오래 걸림. 아직 데이터의 형태에 따라 어떻게 값을
        추출해야 하는지 감을 잘 못잡는 것 같음. 풀긴 했지만 1문제를 푸는데 1시간 가량을 써버림. 향후 시간이 남는다면 강사님께
        코드 리뷰를 부탁드리거나 내가 코드를 다시 확인하고 더 간추릴 수 있는지 확인해 보고싶음.